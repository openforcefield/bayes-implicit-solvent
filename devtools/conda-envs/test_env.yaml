name: test
channels:
  - openeye
  - conda-forge
  - omnia
dependencies:
    # Base depends
  - python
  - pip
  - setuptools
  - networkx
  - tqdm
  - numpy
  - matplotlib
  - openeye-toolkits
  - openmmtools
  - autograd
  - scipy
  - pymbar

    # Testing
  - pytest
  - pytest-cov

    # Pip-only installs
  - pip:
    - codecov